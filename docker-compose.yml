services:
  kmlviewer:
    image: kmlviewer
    build:
      context: .
      dockerfile: Dockerfile
    environment:
      PORT: 5174
      EXTERNAL_GPX_DIR: /maps/gpx
    ports:
      - "5174:5174"
    volumes:
      - D:/Maps/Gpx:/maps/gpx:ro
      - ./gpxes:/app/gpxes
